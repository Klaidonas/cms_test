export type ProductData = {
  id: string,
  photo: string[],
  title: string,
  price: number,
  categproes: string[]
}

export type ProductCategories = {
  id: string;
  category: string;
}
